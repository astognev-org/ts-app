name: Release on merge

on:
  push:
    branches: [main]

jobs:
  release:
    uses: your-org/devops/.github/workflows/release.yml@main
    with:
      head_sha: ${{ github.sha }}
    permissions:
      contents: write
      pull-requests: read
      packages: write
    secrets: inherit
